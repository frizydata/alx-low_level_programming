#include "main.h"
/*
 * mul - multiplies two integers
 * @a: integer to be multipled with b
 * @b: integer to be multipled with a
 * Return: Always 0 (Success: The multipled value)
 */

int mul(int a, int b)
{
	return (a * b);
}
