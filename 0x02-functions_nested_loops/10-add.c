#include "main.h"
/**
 * add - Adds two integers
 * @a: Parameter to be added to b
 * @b: Parameter to be added to a
 * Return: sum of integers
 */

int add(int a, int b)
{
	int adding;

	adding = a + b;

	return (adding);
}
